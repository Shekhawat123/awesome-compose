filebeat.inputs:
  - type: log
    enabled: true
    paths:
      - /app/logs/app.log
    fields:
      service: backend
    multiline.pattern: '^\d{4}-\d{2}-\d{2}'
    multiline.negate: true
    multiline.match: after

output.logstash:
  hosts: ["logstash:5044"]
